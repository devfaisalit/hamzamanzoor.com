version: '3'
services:
  react-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
      - ./public:/app/public
    environment:
      - REACT_APP_NAME="Hamza Manzoor"
    
